const selectAngle = (state) => state.angle;
const selectLength = (state) => state.length;
const selectAttraction = (state) => state.attraction;

export {
  selectAngle,
  selectLength,
  selectAttraction,
};
